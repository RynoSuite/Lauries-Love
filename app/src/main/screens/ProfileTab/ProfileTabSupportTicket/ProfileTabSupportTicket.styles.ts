import { StyleSheet } from 'react-native';
import colors from 'styles/colors';

export default StyleSheet.create({
  container: { flex: 1, paddingHorizontal: 16 },
  scroll: { paddingBottom: 140 },
  subject: {
    fontSize: 20,
    fontWeight: '700',
    color: colors.heading,
    marginTop: 8,
  },
  cat: { fontSize: 13, color: colors.faint, marginBottom: 16 },
  k: {
    fontSize: 12,
    textTransform: 'uppercase',
    letterSpacing: 0.4,
    color: colors.faint,
    marginBottom: 6,
    marginTop: 18,
  },
  desc: {
    fontSize: 15,
    lineHeight: 22,
    color: colors.heading,
    backgroundColor: colors.surface,
    borderWidth: 1,
    borderColor: colors.line,
    borderRadius: 12,
    padding: 12,
  },
  contact: { fontSize: 15, color: colors.heading, marginBottom: 2 },
  link: { color: colors.secondary[600] },
  mono: { fontSize: 12, color: colors.faint },
  segment: { flexDirection: 'row', gap: 8 },
  segBtn: {
    flex: 1,
    paddingVertical: 10,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: colors.line,
    backgroundColor: colors.surface,
    alignItems: 'center',
  },
  segActive: {
    borderColor: colors.primary[500],
    backgroundColor: colors.surface2,
  },
  segText: { fontSize: 13, color: colors.muted },
  segTextActive: { color: colors.heading, fontWeight: '700' },
  assigneeRow: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: 8,
  },
  pill: {
    paddingVertical: 8,
    paddingHorizontal: 14,
    borderRadius: 999,
    borderWidth: 1,
    borderColor: colors.line,
    backgroundColor: colors.surface,
  },
  pillActive: {
    borderColor: colors.primary[500],
    backgroundColor: colors.surface2,
  },
  pillText: { fontSize: 13, color: colors.muted },
  pillTextActive: { color: colors.heading, fontWeight: '600' },
  actions: { marginTop: 26, gap: 10 },
  loader: { marginTop: 80 },
});
